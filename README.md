# Technik - Made with Flask

To run on your local machine, install the latest requirements as :
`pip install -r requirements.txt`

Change your directory and run using python command :
`py main.py`

Visit http://localhost:5000
